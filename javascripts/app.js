define(['jquery',
        'underscore',
        'jquerymobile',
        'backbone',
        'mustache'],
function($, _, a, Backbone, Mustache) {
  return {
    initialize: function() {
    }
  };
});
